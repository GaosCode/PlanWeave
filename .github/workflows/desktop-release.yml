name: Desktop Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: Desktop package version to release, without the leading v.
        required: true
        type: string
      signed:
        description: Build signed installers and require release credentials.
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      publish:
        description: Upload artifacts to the GitHub Release after all builds pass.
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    environment: desktop-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS universal
            runner: macos-26
            builderArgs: --mac dmg zip --universal
            artifactName: macos-universal
            releasePlatform: mac
          - name: Linux x64
            runner: ubuntu-latest
            builderArgs: --linux AppImage deb tar.gz --x64
            artifactName: linux-x64
          - name: Windows x64
            runner: windows-latest
            builderArgs: --win nsis zip --x64
            artifactName: windows-x64
            releasePlatform: win

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.32.1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 26
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify desktop version
        env:
          DESKTOP_VERSION: ${{ inputs.version }}
        run: node -e "const pkg = require('./packages/desktop/package.json'); if (pkg.version !== process.env.DESKTOP_VERSION) { throw new Error('Expected desktop version ' + process.env.DESKTOP_VERSION + ', found ' + pkg.version); }"

      - name: Build desktop app
        run: pnpm --dir packages/desktop build

      - name: Generate release build metadata
        run: pnpm --dir packages/desktop exec tsx scripts/generate-build-metadata.ts --channel release --signed-distribution ${{ inputs.signed == 'true' && matrix.artifactName != 'linux-x64' }}

      - name: Materialize macOS notarization API key
        if: inputs.signed == 'true' && matrix.releasePlatform == 'mac'
        shell: bash
        env:
          APPLE_API_KEY_BASE64: ${{ secrets.APPLE_API_KEY }}
        run: |
          set -euo pipefail
          key_path="${RUNNER_TEMP}/planweave-notarization-key.p8"
          umask 077
          printf '%s' "${APPLE_API_KEY_BASE64}" | base64 -D > "${key_path}"
          chmod 600 "${key_path}"
          test -s "${key_path}"
          printf 'APPLE_API_KEY=%s\n' "${key_path}" >> "${GITHUB_ENV}"

      - name: Preflight macOS release secrets
        if: inputs.signed == 'true' && matrix.releasePlatform == 'mac'
        env:
          CSC_LINK: ${{ secrets.MACOS_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.MACOS_CSC_KEY_PASSWORD }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
        run: node packages/desktop/scripts/preflight-release-secrets.mjs --platform mac

      - name: Preflight Windows release secrets
        if: inputs.signed == 'true' && matrix.releasePlatform == 'win'
        env:
          WIN_CSC_LINK: ${{ secrets.WINDOWS_OV_PFX }}
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WINDOWS_OV_PFX_PASSWORD }}
        run: node packages/desktop/scripts/preflight-release-secrets.mjs --platform win

      - name: Build unsigned installers
        if: inputs.signed == 'false' || matrix.artifactName == 'linux-x64'
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: pnpm --dir packages/desktop exec electron-builder ${{ matrix.builderArgs }} --publish never

      - name: Build signed macOS installers
        if: inputs.signed == 'true' && matrix.releasePlatform == 'mac'
        env:
          CSC_LINK: ${{ secrets.MACOS_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.MACOS_CSC_KEY_PASSWORD }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
        run: pnpm --dir packages/desktop exec electron-builder --config electron-builder.release.cjs ${{ matrix.builderArgs }} --publish never

      - name: Remove macOS notarization API key
        if: always() && inputs.signed == 'true' && matrix.releasePlatform == 'mac'
        shell: bash
        run: rm -f -- "${RUNNER_TEMP}/planweave-notarization-key.p8"

      - name: Build signed Windows installers with OV PFX
        if: inputs.signed == 'true' && matrix.releasePlatform == 'win'
        env:
          WIN_CSC_LINK: ${{ secrets.WINDOWS_OV_PFX }}
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WINDOWS_OV_PFX_PASSWORD }}
        run: pnpm --dir packages/desktop exec electron-builder --config electron-builder.release.cjs ${{ matrix.builderArgs }} --publish never

      - name: Install Linux smoke dependencies
        if: matrix.artifactName == 'linux-x64'
        run: sudo apt-get update && sudo apt-get install -y xvfb

      - name: Verify Linux packaged startup
        if: matrix.artifactName == 'linux-x64'
        run: xvfb-run -a node packages/desktop/scripts/verify-packaged-app.mjs

      - name: Verify Linux release package metadata
        if: matrix.artifactName == 'linux-x64'
        shell: bash
        env:
          DESKTOP_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          shopt -s nullglob
          debs=(packages/desktop/release/*.deb)
          if [[ ${#debs[@]} -ne 1 ]]; then
            echo "Expected exactly one Debian package; found ${#debs[@]}." >&2
            exit 1
          fi
          package_name="$(dpkg-deb -f "${debs[0]}" Package)"
          package_version="$(dpkg-deb -f "${debs[0]}" Version)"
          package_architecture="$(dpkg-deb -f "${debs[0]}" Architecture)"
          test -n "${package_name}"
          if [[ "${package_version}" != "${DESKTOP_VERSION}" ]]; then
            echo "Debian package version '${package_version}' does not match '${DESKTOP_VERSION}'." >&2
            exit 1
          fi
          if [[ "${package_architecture}" != "amd64" ]]; then
            echo "Debian package architecture '${package_architecture}' is not 'amd64'." >&2
            exit 1
          fi

      - name: Verify macOS universal binary
        if: matrix.artifactName == 'macos-universal'
        run: |
          set -euo pipefail
          app_binary="$(find packages/desktop/release -path '*/PlanWeave.app/Contents/MacOS/PlanWeave' -type f | head -n 1)"
          test -n "${app_binary}"
          lipo -info "${app_binary}" | tee /tmp/planweave-lipo.txt
          grep -Eq 'x86_64|x64' /tmp/planweave-lipo.txt
          grep -q 'arm64' /tmp/planweave-lipo.txt

      - name: Verify unsigned macOS packaged startup
        if: inputs.signed == 'false' && matrix.releasePlatform == 'mac'
        run: node packages/desktop/scripts/verify-packaged-app.mjs

      - name: Verify notarized macOS release and installed startup
        if: inputs.signed == 'true' && matrix.releasePlatform == 'mac'
        run: node packages/desktop/scripts/verify-macos-release.mjs

      - name: Verify unsigned Windows packaged startup
        if: inputs.signed == 'false' && matrix.releasePlatform == 'win'
        run: node packages/desktop/scripts/verify-packaged-app.mjs

      - name: Verify signed Windows release and installed startup
        if: inputs.signed == 'true' && matrix.releasePlatform == 'win'
        shell: pwsh
        run: ./packages/desktop/scripts/verify-windows-release.ps1

      - name: Upload desktop artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desktop-${{ matrix.artifactName }}
          if-no-files-found: error
          path: |
            packages/desktop/release/*.AppImage
            packages/desktop/release/*.blockmap
            packages/desktop/release/*.deb
            packages/desktop/release/*.dmg
            packages/desktop/release/*.exe
            packages/desktop/release/*.tar.gz
            packages/desktop/release/verification-*.txt
            packages/desktop/release/*.yml
            !packages/desktop/release/builder-debug.yml
            packages/desktop/release/*.zip

  verify-assets:
    name: Verify release artifacts
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download desktop artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: desktop-*
          path: desktop-artifacts
          merge-multiple: true

      - name: Verify required assets
        env:
          VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          find desktop-artifacts -maxdepth 1 -type f -printf '%f\n' | sort
          node packages/desktop/scripts/verify-release-assets.mjs --dir desktop-artifacts --version "${VERSION}"

  publish:
    name: Publish GitHub Release assets
    runs-on: ubuntu-latest
    needs: verify-assets
    if: inputs.publish == 'true'
    permissions:
      contents: write

    steps:
      - name: Download desktop artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: desktop-*
          path: desktop-artifacts
          merge-multiple: true

      - name: Upload release assets
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          tag="v${VERSION}"
          if ! gh release view "${tag}" >/dev/null 2>&1; then
            gh release create "${tag}" --target "${GITHUB_SHA}" --title "${VERSION}" --generate-notes
          fi
          gh release upload "${tag}" desktop-artifacts/* --clobber
          gh release view "${tag}" --json tagName,name,isDraft,isPrerelease,url,assets
